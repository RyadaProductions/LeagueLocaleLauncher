namespace LeagueLocaleLauncher
{
    // TODO: check what regions are actually supported, remove the rest
    public enum Region
    {
        BR,
        EUNE,
        EUW,
        JP,
        KR, // doesn't work
        LA1,
        LA2,
        NA,
        OC1,
        PBE, // doesn't work
        RU,
        TR,
    }
}
